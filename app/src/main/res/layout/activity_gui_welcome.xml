<?xml version="1.0" encoding="utf-8"?>
<android.support.design.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true"
    tools:context="fr.enseeiht.superjumpingsumokart.application.GUIWelcom">

    <android.support.design.widget.AppBarLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:theme="@style/AppTheme.AppBarOverlay">

        <android.support.v7.widget.Toolbar
            android:id="@+id/toolbar"
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize"
            android:background="?attr/colorPrimary"
            app:popupTheme="@style/AppTheme.PopupOverlay" />

    </android.support.design.widget.AppBarLayout>

    <android.support.design.widget.FloatingActionButton
        android:id="@+id/fab"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|end"
        android:layout_margin="@dimen/fab_margin"
        app:srcCompat="@android:drawable/ic_dialog_email" />

    <Button
        android:text="Connexion BT"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/btConnexionBtn"
        app:layout_anchor="@+id/include"
        app:layout_anchorGravity="center_vertical|center_horizontal"
        android:layout_gravity="center_vertical|center_horizontal" />

    <include layout="@layout/content_menu"
        android:id="@+id/include" />

    <Button
        android:text="Connexion WIFI"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/wifiConnexionBtn"
        app:layout_anchor="@+id/include"
        app:layout_anchorGravity="center_vertical|left"
        android:layout_gravity="center_vertical|right" />

    <Button
        android:text="Start Race"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/startRaceBtn"
        app:layout_anchor="@+id/include"
        app:layout_anchorGravity="center_vertical|right"
        android:layout_gravity="center_vertical|left" />

    <Button
        android:text="Set Circuit"
        android:layout_width="wrap_content"
        android:layout_height="62dp"
        android:id="@+id/setCircuitBtn"
        app:layout_anchor="@+id/include"
        app:layout_anchorGravity="bottom|center_horizontal"
        android:layout_gravity="top|center_horizontal" />

</android.support.design.widget.CoordinatorLayout>
